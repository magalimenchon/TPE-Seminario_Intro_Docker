version: '3'
services:
  ruby:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/myapp/
    depends_on:
      - redis
  redis:
    image: "redis"
    ports: 
      - 6379:6379
    